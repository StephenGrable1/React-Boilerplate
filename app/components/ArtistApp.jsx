'use strict';

var React = require('react');

var ArtistApp = React.createClass({
  render() {
    return(
      <div>
        <h1>This is main ArtistApp which will render search component and artist list component </h1>
      </div>
    );
  }
});

module.exports = ArtistApp;
